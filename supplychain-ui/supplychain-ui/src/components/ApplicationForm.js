import React from 'react';
import AddApplication from './AddApplication';  // Fixed import path (removed /components)

const ApplicationForm = () => {
  return (
    <AddApplication />
  );
};

export default ApplicationForm;